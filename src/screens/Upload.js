import React from "react";
import { Text, View } from "react-native";

const Upload = () => {
    return (
        <View>
            <Text>MyPage</Text>
        </View>
    );
};

export default Upload;
